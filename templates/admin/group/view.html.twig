{% extends 'base.html.twig' %}
{% block body %}

    <h1>Group "{{ group.name }}" infos:</h1>
    <div class="container-fluid bg-light rounded p-3 ">
        <div class="row">
            <div class="col-3 col-sm-3 placeholder text-center">
                <img src="data:image/gif;base64,R0lGODlhAQABAIABAAJ12AAAACwAAAAAAQABAAACAkQBADs=" width="200"
                     height="200" class="img-fluid rounded-circle" alt="Generic placeholder thumbnail">
                <h4>{{ group.name | upper }}</h4>
                <div class="text-muted">TOP 3 | TOP 10</div>
                <hr>
                <div>
                    <a href="{{ path('group_check', {'id': group.id}) }}">
                        <button class="btn btn-success">⟳ CHECK RANK</button>
                    </a>
                </div>
            </div>
            <div class="col-4 col-sm-4 text-center">
                <h3>Targets</h3>
                <hr>
                {% for target in targets %}
                    {{ target.name }}:
                    <a href="{{ path('target_show',{'id':target.id}) }}">{{ target.pattern }}</a><br>
                {% endfor %}
            </div>
            <div class="col-4 col-sm-4  text-center">
                <h3>Searches</h3>
                <hr>
                {% for search in searches %}
                    <a href="{{ path('search_show',{'id':search.id}) }}">{{ search.keyword }}</a><br>
                {% endfor %}
            </div>
            <div class="col-1 col-sm-1">
                <div class="text-right">
                    <a href="{{ path('group_delete', {'id': group.id}) }}">
                        <button class="btn btn-danger">✕</button>
                    </a>
                </div>

            </div>
        </div>

    </div><br>
 {#   <h2 class="text-center">Ranks:</h2>
    <hr>
    <div class="container bg-light rounded p-3 ">
        <table class="table-hover table table-responsive-xl">
            <thead>
            <tr>
                <th>#</th>
                <th>URL</th>
                <th>Keywords</th>
            </tr>
            </thead>
            <tbody>
            {% if ranks is defined %}
                {% for rank in ranks %}
                    <tr>
                        <td>{{ rank.rank }}</td>
                        <td>
                            {% if rank.target is not null %}
                                {{ rank.target.pattern }}
                            {% else %}-
                            {% endif %}</td>
                        <td>i{{ rank.search.keyword }}</td>
                    </tr>
                {% endfor %}
            {% else %}
                <tr>
                    <td>Check</td>
                    <td>your</td>
                    <td>rank</td>
                </tr>
            {% endif %}

            </tbody>
        </table>
    </div>#}
{% endblock %}