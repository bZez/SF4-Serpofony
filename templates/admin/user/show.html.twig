{% extends 'base.html.twig' %}
{% block body %}
    <h1>User {{ user.id }} info:</h1>
    <table class="table">
        <thead>
        <tr>
            <th>id</th>
            <th>Mail</th>
            <th>Username</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.email }}</td>
                <td>{{ user.username }}</td>
                <td>
                    <a href="/admin/users/delete/{{ user.id }}"><button>✕</button></a>

                </td>
            </tr>

        </tbody>
    </table>
    {{ form_start(form) }}
    {{ form_widget(form.username) }}
    {{ form_widget(form.email) }}
    <button type="submit">Register!</button>
    {{ form_end(form) }}
{% endblock %}